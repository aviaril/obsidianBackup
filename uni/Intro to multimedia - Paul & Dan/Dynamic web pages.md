[[HTML5]]
- content is changed in response to conditions 
	- this can be done by client or server side scripting 

#### server side scripting 
- page requests are fulfilled by running a script on the server to generate/ alter an html page to give to the client 
- there are many languages and technologies to do this 
	- PHP
		- info
			- Script is embedded in the HTML and run when the page is requested 
			- open source 
			- created in 1995
		- Use 
			- only parses code in the delimiter tags 
			- \<?php echo '\<p>Hello World; ?>
	- ASP.net
	- Python 
	- JSP
		- java server pages 
##### Server side applications 
- mostly antiquated now 
- Common Gateway Interface (CGI) 
- Client sends a request to a server that runs a separate application, the server then interprets the output and sends it back to the client in a format the client can use

#### Client side 
- Program is run in the browser on the client machine 
- small programs in the source form of an interpreted language are embedded in 
- you know what client side is 
