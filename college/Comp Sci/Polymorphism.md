- OOP concept refers to the ability of a variable function or object to take on multiple forms 
- Polymorphism allows you to 

#### examples 
- e.g. super class called Animals 
	- subclasses horse, fish, bird 
	- Animals have function called move but each subclass moves differently 
	- with polymorphism each subclass can have its own way of implementing the function 
- e.g. superclass Shapes 
	- shapes has a method called area that returns the area 
	- each shape needs a different equation 

